// src/config/constants.ts

/** Default DPI for print export */
export const DEFAULT_DPI = 300;

/** Default template size in pixels */
export const DEFAULT_TEMPLATE_PX = 4096;

/** Decal mesh depth for 3D rendering */
export const DECAL_DEPTH = 110;

/** World-scale approximation across products */
export const WORLD_ZONE_W = 0.9;

/** Minimum scale for artwork */
export const MIN_SCALE = 0.05;

/** Maximum scale for artwork */
export const MAX_SCALE = 1.2;

/** Default initial scale for new artwork */
export const DEFAULT_SCALE = 0.3;

/** Snap threshold for center snapping */
export const CENTER_SNAP_THRESHOLD = 0.02;

/** Default grid size in cm */
export const DEFAULT_GRID_CM = 1;
